// Crie uma função que retorne o triplo do número recebido no parâmetro da função
const triplicando = function (number) {
	return number * 3;
};

console.log(triplicando(2));
console.log(triplicando(3));
console.log(triplicando(9));
